# 📋 구글 폼에 그대로 붙여넣을 내용

---

## 팀명
```
다시봄
```

## 작품명 (40자 이내)
```
천안 균형발전 나침반 — 생활권 격차 진단과 생활SOC 투자 우선순위
```
> 38자입니다. 파일명이 길어져 부담되시면 짧은 버전: `천안 균형발전 나침반` (10자)

---

## 활용 데이터 목록
> ※ 쉼표로 구분

```
행정안전부_주민등록 인구통계(연령별 인구현황), 소상공인시장진흥공단_상가(상권)정보, 국가데이터처_미거주주택(빈집)비율(시도/시/군/구)
```

## 활용 데이터 URL
> ※ 쉼표로 구분

```
https://jumin.mois.go.kr/, https://www.data.go.kr/data/15083033/fileData.do, https://kosis.kr/statHtml/statHtml.do?orgId=101&tblId=DT_1YL202005&conn_path=I3
```

> ⚠️ **가운데 상가정보 URL만 5초 확인 부탁드립니다.**
> 브라우저 방문기록에서 `소상공인시장진흥공단_상가(상권)정보` 페이지 주소를 확인해
> 다르면 그 주소로 바꿔 주세요. 나머지 둘은 확인된 주소입니다.

---

## 파일 업로드

**`다시봄_천안 균형발전 나침반.zip`** (4.7MB) 하나만 올리시면 됩니다.
요구하신 `팀명_작품명` 규칙에 맞춰 두었고, 100MB 제한 안에 넉넉히 들어갑니다.

압축 안에 든 것:

| 파일 | 내용 |
|---|---|
| `[기획서] ….pdf` | **데이터 분석 기획서 21장** — 심사위원이 먼저 볼 파일 |
| `[기획서] ….pptx` | 같은 내용 편집 가능본 |
| `[시각화] 대시보드.html` | 브라우저로 여는 전체 분석 (인터넷 없이 열림) |
| `[시각화] 그림/` | 기획서에 쓰인 그림 8장 원본 |
| `[참고] 분석표/` | 지표 원표·가중치·모델 성능·데이터 출처 |
| `README.txt` | 무엇부터 보면 되는지 안내 |

> **PDF·PPTX·HTML 전부 넣는 게 맞습니다.** 요강이 "압축하여 업로드"라고 한 것은
> 여러 파일을 함께 내라는 뜻이고, 시각화 파일도 별도로 요구하고 있습니다.

---

## 버스 노선 데이터는 뺐습니다

주신 `충청남도 천안시_시내버스운수업체별노선현황` 은 **노선 단위(154개 노선)** 라
정류장 좌표도 읍면동도 없어 생활권별 이동성 지표로 쓰지 못했습니다.

**쓰지 않은 데이터를 활용 목록에 적으면 안 됩니다.** 심사에서 "이 데이터로 무엇을
산출했나" 물으면 답할 수 없기 때문입니다. 그래서 목록에서 뺐습니다.

> 혹시 `천안시 버스정류소` 로 검색해 **정류소 위치(좌표 또는 주소)** 가 담긴 파일을
> 구하시면 5개 축이 다 찹니다. 없어도 제출에는 문제없습니다.
